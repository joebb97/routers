# routers
Linux routing and firewalling daemon - for fun!

Does everything systemd-networkd, ifupdown, nft, and iproute2 do. All within one monolithic daemon.

The project ends in "rs" as a nod to Rust, the language it is written in.
